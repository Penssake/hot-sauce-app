const Footer = ({ text, number }) => {
    return (
        <footer className="hot-sauce-footer">
            <h3>{text}</h3>
            <p>{number}</p>
        </footer>
    )
}

export default Footer;
